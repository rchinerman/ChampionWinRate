import styled from "styled-components"

export const Container = styled.div`
  display: flex;
  flex-wrap: wrap;
  max-height: inherit;
  align-items: center;
  justify-content: center;
  overflow: auto;
`
